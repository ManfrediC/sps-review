# config / prompts / examples

Few-shot example JSON assets referenced by prompt templates.

These files are generated from curated rows under `examples/` by:

```bash
python src/pipelines/09_build_langextract_examples.py
```

Do not hand-edit generated examples unless the corresponding curated source row
or generator logic is also updated.
