# config / schema

JSON Schemas used to validate structured model outputs and related extraction
records.

Current schema files:

- `SPS_case_extraction.schema.json`
- `SPS_cohort_extraction.schema.json`
- `SPS_quality_assessment.schema.json`

Keep schema changes aligned with the dictionaries in `config/dictionaries/` and
the relevant prompt templates in `config/prompts/`.
