# config / dictionaries

CSV dictionaries that define extraction fields, coding rules, and expected
value semantics.

Current dictionary files:

- `SPS_column_dictionary.csv`
- `SPS_cohort_dictionary.csv`
- `SPS_quality_dictionary.csv`

These files are the human-readable source of truth for field meanings. Keep
schema and prompt changes aligned with them.
