# config / dictionaries

## Purpose
CSV dictionaries that define extraction fields, coding rules, and expected value semantics.

## Directory Contents Snapshot
- Last updated: `2026-03-05`
- Immediate subdirectories (0): _None_
- Immediate files (3, excluding `README.md`): `SPS_cohort_dictionary.csv`, `SPS_column_dictionary.csv`, `SPS_quality_dictionary.csv`
