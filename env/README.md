# env

Ignored local environment files and machine-specific runtime configuration.

Typical local files include API keys or login details such as
`openai_api_key.env` or Covidence credentials. These files must stay untracked.

Never place secrets in tracked config, prompts, logs, or generated artefacts.
