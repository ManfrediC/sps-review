# Stage 06 Follow-up Notes

- `8182` is currently linked to the wrong abstract in the local proceedings text. Treat the existing stage-06 outputs for this paper as non-final, recover the correct source PDF/abstract, and rerun the paper from scratch once the source is fixed.
- Create a dedicated later-stage deduplication script before final patient-level synthesis so overlapping patients or reused cohorts can be detected across papers and not counted multiple times.
