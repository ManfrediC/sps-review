# `src` / `autoresearch`

## Purpose
Placeholder for future bounded optimisation harnesses.

There are currently no active autoresearch harnesses under `src/`.

The retired stage-05 proceedings-trimming bundle now lives under `legacy/stage_05_autoresearch/`. That archive preserves:
- the frozen harness code and prompt files
- the isolated stage-05 `_autoresearch` pipeline copies
- the dedicated test files
- the non-canonical watcher logs and run artefacts

Production and review workflows should continue to use `src/pipelines/` and `src/validation/`.
